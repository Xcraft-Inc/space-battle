export default function styles() {
  const ships = {
    display: 'grid',
  };

  const ship = {
    border: 1,
    borderColor: 'blue',
    padding: '5px',
  };

  return {
    ships,
    ship,
  };
}
